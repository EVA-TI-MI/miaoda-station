# 小说爬虫 Novel Crawler v3.0

一个用 Python 编写的多站点小说下载工具，支持笔趣阁类网站、国学荟萃及通用章节页面。

## 功能特点

- **自动站点检测**：默认根据 URL 自动匹配适配器，无需手动指定
- 多站点适配：内置国学荟萃、笔趣阁模板、通用自动识别三种适配器（通用适配器自动回退 h1/#info 识别书名作者）
- 异步并发下载（基于 aiohttp），可自定义并发数
- 请求延时控制，礼貌爬取，降低被封风险
- 断点续传 / **增量更新**：中断或更新后重新运行自动跳过已下载章节、只补新章（`--update`）
- 失败自动重试（指数退避）+ **终轮顺序补抓**，仍失败的章节写入 `<书名>.failed.txt` 清单
- **智能编码检测**：按 BOM / meta charset / chardet 自动识别，UTF-8 与 GBK/GB18030 老站通吃，也可用 `-e` 手动指定
- **章节分页自动合并**：同一章被拆成 `123.html / 123_2.html` 时自动拼接（只认同章节序号分页，不会误并下一章；`--no-merge-pages` 可关闭）
- 导出 **TXT / EPUB 电子书**（`--format txt|epub|both`，EPUB 用标准库生成、无需额外依赖）+ meta.json 元数据
- **关键词搜索**：`--search 关键词 --search-site "搜索入口URL"`（入口中用 {q} 占位关键词）
- **批量下载**：`-l urls.txt`，一行一个目录页 URL，`#` 开头为注释
- **自定义代理 / UA / Referer / Cookie**（`--proxy/--ua/--referer/--cookie`）
- 实时进度条显示
- **一键流水线**：`--web` 参数自动完成 爬取 → 文本修正 → 生成网页数据
- 配套文本修正工具（fix_text.py）：自动修复源站 OCR 错字和生僻字替换
- 配套数据构建工具（build_data.py）：将 TXT 转为网页可用的 JS 数据文件，自动规范化章节标题（第001回 → 第一回）

## 安装依赖

```bash
pip install -r requirements.txt
```

或手动安装：

```bash
pip install aiohttp beautifulsoup4
```

## 使用方法

### 基本用法

```bash
# 自动检测站点并下载（推荐，无需指定适配器）
python novel_crawler.py -u <小说目录页URL>

# 一键完成：爬取 + 文本修正 + 生成网页数据
python novel_crawler.py -u <小说目录页URL> --web
```

### 参数说明

| 参数 | 缩写 | 说明 | 默认值 |
|------|------|------|--------|
| `--url` | `-u` | 小说目录页 URL（必填） | - |
| `--adapter` | `-a` | 适配器：auto/biquge/generic/guoxue | **auto** |
| `--output` | `-o` | TXT 输出目录 | novels |
| `--delay` | `-d` | 每章请求间隔（秒） | 1.0 |
| `--concurrent` | `-c` | 最大并发数 | 3 |
| `--timeout` | `-t` | 请求超时（秒） | 15 |
| `--retries` | `-r` | 失败重试次数 | 3 |
| `--encoding` | `-e` | 指定页面编码（如 gbk），不指定则自动检测 | 自动 |
| `--format` | | 导出格式：txt / epub / both | txt |
| `--update` | | 增量更新：已下载章节走缓存，只补新章 | 关 |
| `--no-merge-pages` | | 关闭章节分页自动合并 | 关 |
| `--list` | `-l` | 批量下载清单文件（一行一个 URL） | - |
| `--proxy` | | HTTP/SOCKS 代理，如 http://127.0.0.1:7890 | - |
| `--ua` | | 自定义 User-Agent | 内置 |
| `--referer` | | 自定义 Referer | - |
| `--cookie` | | 自定义 Cookie | - |
| `--search` | | 关键词搜索（配合 --search-site） | - |
| `--search-site` | | 站点搜索入口，{q} 为关键词占位 | - |
| `--fix` | | 爬取后自动运行 fix_text.py 修正文本 | 关 |
| `--build` | | 爬取后自动运行 build_data.py 生成网页数据 | 关 |
| `--web` | | **一键流程**：爬取 + 修正 + 生成网页数据 | 关 |
| `--web-dir` | | 网页数据输出目录（biquge.html 所在目录） | 脚本目录 |

> `-u` / `-l` / `--search` 三选一：单本下载、批量下载或关键词搜索。

### 适配器说明

| 适配器 | 适用站点 | 目录页特征 | 正文特征 |
|--------|---------|-----------|---------|
| `biquge` | 笔趣阁模板站 | `div#list` + `dd>a` | `div#content` |
| `guoxue` | 国学荟萃 (guoxuehuicui.com) | "第XXX回"链接列表 | `div.main-content` 内 `<p>` |
| `generic` | 其他通用站点 | 自动尝试多种选择器 | 自动尝试多种选择器 |

### 使用示例

```bash
# 自动检测站点并下载（推荐）
python novel_crawler.py -u https://www.guoxuehuicui.com/novel/sanguoyanyi/

# 一键流程：爬取 + 修正 + 生成网页数据到 D:\tanlan
python novel_crawler.py -u https://www.guoxuehuicui.com/novel/sanguoyanyi/ --web --web-dir D:\tanlan

# 指定适配器
python novel_crawler.py -u https://example.com/book/123/ -a biquge
python novel_crawler.py -u https://example.com/novel/456 -a generic

# 高并发 + 短延时（速度快，但更容易被封，谨慎使用）
python novel_crawler.py -u URL -c 5 -d 0.5

# 低并发 + 长延时（更安全，适合反爬严格的站点）
python novel_crawler.py -u URL -c 1 -d 3.0

# 手动指定 GBK 编码（一般无需，已能自动识别）
python novel_crawler.py -u URL -e gbk

# 导出 EPUB 电子书（手机阅读软件可直接打开）
python novel_crawler.py -u URL --format epub
# 同时导出 TXT 和 EPUB
python novel_crawler.py -u URL --format both

# 增量更新已下载的书（只补新章节）
python novel_crawler.py -u URL --update

# 批量下载：urls.txt 一行一个目录页
python novel_crawler.py -l urls.txt -o novels

# 走本地代理 + 自定义 UA
python novel_crawler.py -u URL --proxy http://127.0.0.1:7890 --ua "Mozilla/5.0"

# 关键词搜索（先拿到书籍目录页 URL，再用 -u 下载）
python novel_crawler.py --search 三国演义 --search-site "https://example.com/search?q={q}"

# 指定输出目录
python novel_crawler.py -u URL -o D:\books
```

## 文本修正（fix_text.py）

部分源站存在 OCR 错字（如"曹躁"代"曹操"）和生僻字替换（如"-"代"傕/彧/瓘"）。
`fix_text.py` 可自动修正这些问题：

```bash
# 修正单个文件
python fix_text.py novels/三国演义.txt

# 批量修正目录下所有 TXT
python fix_text.py novels/
```

修正内容包括：
- "躁"系统性替换为"操"（保护急躁/浮躁/焦躁/暴躁等合法词）
- 生僻字"-"还原为正确人名（荀彧/李傕/夏侯惇/张郃/卫瓘等数十人）
- 地名还原（郿坞/猇亭/段谷/上邽/琅琊/越巂等）
- OCR 错字修正（荒滢→荒淫、牢蚤→牢骚、貌貅→貔貅等）

## 生成网页数据（build_data.py）

将下载的 TXT 小说转换为 biquge.html 可加载的 `novels_data.js`：

```bash
# 从 novels/ 目录读取 TXT，生成 novels_data.js
python build_data.py -i novels -o D:\tanlan

# 生成演示数据（公版名著节选，不需要爬虫）
python build_data.py --demo -o D:\tanlan
```

生成的 `novels_data.js` 放在 biquge.html 同目录下，刷新网页即可看到真实小说内容。

## 完整工作流

### 方式一：一键完成（推荐）

```bash
# 一条命令完成爬取、修正、生成网页数据
python novel_crawler.py -u https://www.guoxuehuicui.com/novel/sanguoyanyi/ --web --web-dir D:\tanlan

# 然后双击打开 D:\tanlan\biquge.html 即可阅读
```

### 方式二：分步执行

```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 爬取公版小说（自动检测站点）
python novel_crawler.py -u https://www.guoxuehuicui.com/novel/sanguoyanyi/ -o novels -d 0.8 -c 3

# 3. 修正文本错字
python fix_text.py novels/三国演义.txt

# 4. 生成网页数据
python build_data.py -i novels -o D:\tanlan

# 5. 打开 biquge.html 即可阅读真实小说内容
```

## 输出文件

```
D:\tanlan/
├── biquge.html           # 笔趣阁小说阅读网页
├── novels_data.js        # 网页数据文件（由 build_data.py 生成）
├── snake.html            # 贪吃蛇游戏
├── index.html            # 首页导航
├── novel_crawler.py      # 爬虫主脚本 v3.0
├── fix_text.py           # 文本修正脚本
├── build_data.py         # 数据构建脚本 v2.0
├── validate_data.py      # 数据验证脚本
├── requirements.txt      # Python 依赖
├── 爬虫使用说明.md        # 本文档
└── novels/               # 下载的小说
    ├── 三国演义.txt       # 合并后的完整小说（--format 含 txt 时）
    ├── 三国演义.epub      # EPUB 电子书（--format epub/both 时）
    ├── 三国演义.meta.json # 元数据（简介、分类、标签，自动生成）
    ├── 三国演义.failed.txt# 失败章节号清单（全部成功时不生成）
    └── .三国演义_cache/   # 章节缓存（用于断点续传，可删除）
```

## 扩展新站点

在 `novel_crawler.py` 中继承 `SiteAdapter` 基类，实现两个方法：

```python
class MySiteAdapter(SiteAdapter):
    name = "mysite"

    def match(self, url):
        return "mysite.com" in url

    def parse_index(self, html, base_url):
        # 解析目录页，返回 {"title", "author", "chapters"}
        ...

    def parse_chapter(self, html):
        # 解析章节页，返回 (标题, 正文)
        ...
```

然后在文件顶部的 `ADAPTERS` 列表中注册该类，并在 argparse 的 `--adapter` choices 中加上名称即可。

## 法律声明

本工具仅供学习编程和网络技术使用。使用时请遵守：

1. 仅下载你有权访问的内容（公版书、作者授权作品等）
2. 遵守目标网站的 robots.txt 和服务条款
3. 合理控制请求频率（建议延时 ≥1 秒，并发 ≤3）
4. 下载内容请勿用于商业用途或非法传播
5. 使用者自行承担因不当使用产生的一切法律责任
